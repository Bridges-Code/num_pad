name: num_pad
description: "A new Flutter package project."
version: 0.0.8
homepage: null

environment:
  sdk: ^3.9.0
  flutter: ">=1.17.0"

dependencies:
  auto_size_text_field: ^2.2.4
  flutter:
    sdk: flutter
  shadcn_ui: ^0.29.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter: null
